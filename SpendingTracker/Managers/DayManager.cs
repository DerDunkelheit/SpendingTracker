namespace SpendingTracker.Managers;

public static class DayManager
{
    // TODO: would be nice to load it from file, so we can change it in app.
    public static readonly decimal DAY_INITIAL_BUDGET = 100;
}